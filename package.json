{
  "name": "Openseasubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service scottlozano/open-sea-subgraph", 
    "create-local": "graph create --node http://localhost:8020/ ScottLozano/Openseasubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ ScottLozano/Openseasubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ScottLozano/Openseasubgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
